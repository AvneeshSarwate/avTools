
TODO
- add state-version index (or id?) for each state
(this will help users detect fine grained changes and adjust properly when using shapes in stateful animations )
- add type safety for metadata if key-schemas are provided