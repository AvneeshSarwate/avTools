[package]
name = "hap_decoder"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rayon = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
snap = "1.1"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
