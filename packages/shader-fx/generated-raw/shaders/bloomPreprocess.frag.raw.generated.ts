// Auto-generated by raw WGSL fragment generator. DO NOT EDIT.
import { CustomShaderEffect, type ShaderSource, type UniformDescriptor, type MaterialHandles, type ShaderUniforms, type Dynamic } from '@avtools/shader-fx/raw';
export const BloomPreprocessVertexSource = `// Auto-generated by raw WGSL fragment generator. DO NOT EDIT.
struct VertexOutput {
  @builtin(position) position: vec4f,
  @location(0) vUV: vec2f,
};

@vertex
fn main(@builtin(vertex_index) index: u32) -> VertexOutput {
  var positions = array<vec2f, 3>(
    vec2f(-1.0, -1.0),
    vec2f(3.0, -1.0),
    vec2f(-1.0, 3.0),
  );
  var uvs = array<vec2f, 3>(
    vec2f(0.0, 1.0),
    vec2f(2.0, 1.0),
    vec2f(0.0, -1.0),
  );
  var out: VertexOutput;
  out.position = vec4f(positions[index], 0.0, 1.0);
  out.vUV = uvs[index];
  return out;
}

`;
export const BloomPreprocessFragmentSources = [
  `// Auto-generated by raw WGSL fragment generator. DO NOT EDIT.
struct BloomPreprocessUniforms {
  blackLevel: f32, // 0.05
  brightness: f32, // 2.0
  threshold: f32, // 0.12
  knee: f32, // 0.5
};

fn pass0(
  uv: vec2f,
  uniforms: BloomPreprocessUniforms,
  src: texture_2d<f32>,
  srcSampler: sampler,
) -> vec4f {
  let color = textureSample(src, srcSampler, uv);

  // Subtract black level and boost brightness
  let isolated = max(color.rgb - vec3f(uniforms.blackLevel), vec3f(0.0)) * uniforms.brightness;

  // Soft knee threshold based on luminance
  let lum = dot(isolated, vec3f(0.2126, 0.7152, 0.0722));
  let t = uniforms.threshold;
  let k = t * uniforms.knee;
  let softEdge = smoothstep(t - k, t + k, lum);
  let bright = isolated * softEdge;

  return vec4f(bright, 1.0);
}

@group(0) @binding(0) var<uniform> uniforms: BloomPreprocessUniforms;
@group(0) @binding(1) var src: texture_2d<f32>;
@group(0) @binding(2) var srcSampler: sampler;

struct FragmentInput {
  @location(0) vUV: vec2f,
};

@fragment
fn main(input: FragmentInput) -> @location(0) vec4f {
  let uv_local = input.vUV;
  let uniforms_value = uniforms;
  let color = pass0(uv_local, uniforms_value, src, srcSampler);
  return color;
}

`, // pass0
] as const;
export const BloomPreprocessPassCount = 1 as const;
export const BloomPreprocessPrimaryTextureName = 'src' as const;

export const BloomPreprocessPassTextureSources = [
  [
    { binding: 'src', source: { kind: 'input', key: 'src' } },
  ],
] as const;

export const BloomPreprocessUniformMeta: UniformDescriptor[] = [
  {
    name: 'blackLevel',
    kind: 'f32',
    bindingName: 'uniforms_blackLevel',
    default: 0.05,
  },
  {
    name: 'brightness',
    kind: 'f32',
    bindingName: 'uniforms_brightness',
    default: 2.0,
  },
  {
    name: 'threshold',
    kind: 'f32',
    bindingName: 'uniforms_threshold',
    default: 0.12,
  },
  {
    name: 'knee',
    kind: 'f32',
    bindingName: 'uniforms_knee',
    default: 0.5,
  },
];

export interface BloomPreprocessUniforms {
  blackLevel: number;
  brightness: number;
  threshold: number;
  knee: number;
}

export const BloomPreprocessUniformsDefaults: BloomPreprocessUniforms = {
  blackLevel: 0,
  brightness: 0,
  threshold: 0,
  knee: 0,
};

const BloomPreprocessUniformsLayoutSize = 16;

function packBloomPreprocessUniforms(floatView: Float32Array, intView: Int32Array, uintView: Uint32Array, floatOffset: number, value: BloomPreprocessUniforms): void {
  {
    const base = floatOffset + 0;
    const raw = value.blackLevel;
    floatView[base] = raw !== undefined ? Number(raw) : 0;
  }
  {
    const base = floatOffset + 1;
    const raw = value.brightness;
    floatView[base] = raw !== undefined ? Number(raw) : 0;
  }
  {
    const base = floatOffset + 2;
    const raw = value.threshold;
    floatView[base] = raw !== undefined ? Number(raw) : 0;
  }
  {
    const base = floatOffset + 3;
    const raw = value.knee;
    floatView[base] = raw !== undefined ? Number(raw) : 0;
  }
}

export type BloomPreprocessTextureName = 'src';
export const BloomPreprocessTextureNames = ['src'] as const;
export interface BloomPreprocessInputs {
  src: ShaderSource;
}

export type BloomPreprocessMaterialHandles = MaterialHandles<BloomPreprocessUniforms, BloomPreprocessTextureName>;

export interface BloomPreprocessMaterialOptions {
  name?: string;
  passIndex?: number;
  sampleMode?: 'nearest' | 'linear';
}

export function createBloomPreprocessMaterial(device: GPUDevice, format: GPUTextureFormat, options: BloomPreprocessMaterialOptions = {}): BloomPreprocessMaterialHandles {
  const passIndex = options.passIndex ?? 0;
  if (passIndex < 0 || passIndex >= 1) {
    throw new Error(`Invalid passIndex ${passIndex} for BloomPreprocess. Expected 0 <= passIndex < 1.`);
  }
  const uniformData = new ArrayBuffer(BloomPreprocessUniformsLayoutSize);
  const uniformFloats = new Float32Array(uniformData);
  const uniformInts = new Int32Array(uniformData);
  const uniformUints = new Uint32Array(uniformData);
  let currentUniforms: BloomPreprocessUniforms = { ...BloomPreprocessUniformsDefaults };
  packBloomPreprocessUniforms(uniformFloats, uniformInts, uniformUints, 0, currentUniforms);
  const uniformBuffer = device.createBuffer({ size: uniformData.byteLength, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });
  device.queue.writeBuffer(uniformBuffer, 0, uniformData);

  const bindGroupLayout = device.createBindGroupLayout({ entries: [
    { binding: 0, visibility: GPUShaderStage.FRAGMENT, buffer: { type: "uniform" } },
    { binding: 1, visibility: GPUShaderStage.FRAGMENT, texture: { sampleType: 'float' } },
    { binding: 2, visibility: GPUShaderStage.FRAGMENT, sampler: { type: 'filtering' } },
  ] });

  const pipelineLayout = device.createPipelineLayout({ bindGroupLayouts: [bindGroupLayout] });
  const vertexModule = device.createShaderModule({ code: BloomPreprocessVertexSource });
  const fragmentModule = device.createShaderModule({ code: BloomPreprocessFragmentSources[passIndex] });

  const pipeline = device.createRenderPipeline({
    layout: pipelineLayout,
    vertex: { module: vertexModule, entryPoint: "main" },
    fragment: { module: fragmentModule, entryPoint: "main", targets: [{ format }] },
    primitive: { topology: "triangle-list", cullMode: "none" },
  });

  const samplers: Record<string, GPUSampler> = {};
  const textureViews: Record<string, GPUTextureView> = {};
  for (const name of BloomPreprocessTextureNames) {
    const filter = options.sampleMode ?? "linear";
    samplers[name] = device.createSampler({ magFilter: filter, minFilter: filter });
    const placeholder = device.createTexture({ size: { width: 1, height: 1 }, format, usage: GPUTextureUsage.TEXTURE_BINDING });
    textureViews[name] = placeholder.createView();
  }

  let bindGroup = device.createBindGroup({
    layout: bindGroupLayout,
    entries: [
      { binding: 0, resource: { buffer: uniformBuffer } },
      { binding: 1, resource: textureViews['src'] },
      { binding: 2, resource: samplers['src'] },
    ],
  });

  const rebuildBindGroup = () => {
    bindGroup = device.createBindGroup({
      layout: bindGroupLayout,
      entries: [
        { binding: 0, resource: { buffer: uniformBuffer } },
        { binding: 1, resource: textureViews['src'] },
        { binding: 2, resource: samplers['src'] },
      ],
    });
  };

  const handles: BloomPreprocessMaterialHandles = {
    pipeline,
    get bindGroup() { return bindGroup; },
    setTexture: (name, texture) => {
      textureViews[name] = texture;
      rebuildBindGroup();
    },
    setUniforms: (updates) => {
      currentUniforms = { ...currentUniforms, ...updates };
      packBloomPreprocessUniforms(uniformFloats, uniformInts, uniformUints, 0, currentUniforms);
      device.queue.writeBuffer(uniformBuffer, 0, uniformData);
    },
  };

  return handles;
}

export class BloomPreprocessEffect extends CustomShaderEffect<BloomPreprocessUniforms, BloomPreprocessInputs> {
  override effectName = 'BloomPreprocess';

  constructor(device: GPUDevice, inputs: BloomPreprocessInputs, width = 1280, height = 720, format: GPUTextureFormat = 'rgba16float', clearColor: GPUColor = { r: 0, g: 0, b: 0, a: 1 }, sampleMode: 'nearest' | 'linear' = 'linear') {
    super(device, inputs, {
      factory: (deviceRef, formatRef, options) => createBloomPreprocessMaterial(deviceRef, formatRef, { ...options, sampleMode }),
      textureInputKeys: ['src'],
      textureBindingKeys: BloomPreprocessTextureNames,
      passTextureSources: BloomPreprocessPassTextureSources,
      passCount: 1,
      primaryTextureKey: 'src',
      width,
      height,
      format,
      clearColor,
      uniformMeta: BloomPreprocessUniformMeta,
    });
    this.setUniforms({
      blackLevel: 0.05,
      brightness: 2.0,
      threshold: 0.12,
      knee: 0.5,
    });
  }

  override setUniforms(uniforms: { blackLevel?: Dynamic<number>, brightness?: Dynamic<number>, threshold?: Dynamic<number>, knee?: Dynamic<number> }): void {
    const record: ShaderUniforms = {};
    if (uniforms.blackLevel !== undefined) {
      record['blackLevel'] = uniforms.blackLevel;
    }
    if (uniforms.brightness !== undefined) {
      record['brightness'] = uniforms.brightness;
    }
    if (uniforms.threshold !== undefined) {
      record['threshold'] = uniforms.threshold;
    }
    if (uniforms.knee !== undefined) {
      record['knee'] = uniforms.knee;
    }
    super.setUniforms(record);
  }
}

